menuconfig LED_SUBSYSTEM
    bool "LED Subsystem"
    default y
	
if LED_SUBSYSTEM

config APP_HEARTBEAT_PERIOD_MS 
    int "LED period (100-2000 ms)"
    range 100 2000
    default 500

endif # LED_SUBSYSTEM

source "Kconfig.zephyr"